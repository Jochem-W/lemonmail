generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["fullTextSearch"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Source {
  DM
  GUILD
}

model Thread {
  id String @id

  userId       String
  active       Boolean? @unique
  source       Source
  closedReason String?
  lastMessage  String
}

model StaffMember {
  id String @id

  addToThread Boolean
}

model User {
  id String @id

  blocked Boolean
}
